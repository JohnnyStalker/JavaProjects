this.getClass().getSimpleName()

this <= Classe atual
getClass() <= Pega a classe para usala em algo
getSimpleName() <= Pega o nome da classe

Utilizei isso apenas para ter um método padrão para obter os nomes das classes no toString, 
pode facilmente ser ser substituindo por uma String padrão com o nome da variável


Class: Video
this.getClass().getSimpleName() <=> "Video"
